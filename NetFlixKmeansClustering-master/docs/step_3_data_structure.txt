Data Structure for Step 1:
----------------------------------------------------------
source file name: src/NetFlixAnalysisCanopyData.java

class NetFlixAnalysisDataAggregate {
    class Mapper() {
        declare ArrayList of CanopyCollection and collect at every
            step;
        
        sequenceReader() {
            read the contents of the output results from step 2 and
                store it in the arrayList
        }

        void Map() {
            for (iterate through all the canopy points from the step 2
                    using the above arrayList) {
                commonuser = MatchCount();
            }
            map (movie_id, list<user_id, rating>:list<canopy,
                user_count);
        }

        void NetflixMovie(String, String) {}

        void MatchCount(String, String) {
            return total number of common users;
        }
    }

    class Reducer() {
        void Reduce() {
            Do nothing, just write output;
        }
    }

    main driver class() {
        setting input, output directory;
        setting input, output format for mapper and reducer;
        setting the number of reducers;
    }
}
